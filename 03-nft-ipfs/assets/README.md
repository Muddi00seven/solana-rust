Drop the image you want to mint as an NFT here, named `image.png`
(`scripts/upload-to-ipfs.ts` looks for exactly that path). Any square PNG
works fine for class - a logo, a screenshot, student artwork, whatever.

This folder's actual image files are gitignored-by-convention (don't commit
large binaries into a teaching repo) - only this note is tracked.
