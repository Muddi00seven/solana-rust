# Same convention as projects 1 and 2 - base58 secret key, generated locally,
# never committed.
DEPLOYER_PRIVATE_KEY=

RPC_URL=https://api.devnet.solana.com

# Pinata (https://pinata.cloud) JWT for uploading the NFT image + metadata
# JSON to IPFS. Free tier is plenty for class. Dashboard -> API Keys -> New
# Key -> copy the JWT (not the API key/secret pair).
PINATA_JWT=
